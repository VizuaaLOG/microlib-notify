{
  "name": "microlib-notify",
  "version": "1.0.0",
  "description": "Display simple notifications to inform your users. They can be displayed from the top/bottom and can have custom classes assigned.",
  "main": "./src/notify.microlib.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "gulp minify",
    "build": "gulp build",
    "lint": "jshint ./src/notify.microlib.js"
  },
  "keywords": [
    "notification",
    "html",
    "micro",
    "library",
    "lightweight"
  ],
  "author": "Thomas Erbe <vizuaalog@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.4.1",
    "jshint": "^2.8.0"
  }
}
